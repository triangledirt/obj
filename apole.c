#include "abit.h"
#include "apole.h"
#include "aobj.h"

abit_t abits_classify(aobj_t obj) {
  return 0;
}

void abits_learn(aobj_t objs[], long objs_size, long type) {
  long idx;
  aobj_t obj;
  for (idx = 0; idx < objs_size; idx++) {
    obj = objs[idx];
    if (obj > 0) {};
  }
}
